#ifndef SERVO_TEST_H
#define SERVO_TEST_H

// Should be YAW test like from 0 to 300 with different speeds, also 1 degree test.

// Same thing about PITCH, but this axis has limits MAX_POS and MIN_POS.
// I think it should looks like that: if servo reach MAX_POS --> YAW servo rotates by 180 degrees and we change position of PITCH servo.


#endif // SERVO_TEST_H
